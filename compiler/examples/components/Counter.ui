# Counter.ui - Simple counter component

$count:0

.counter
  h1["Count:  @{$count}"]
  .buttons
    button["-" @ $count--]
    button["Reset" @ $count:0]
    button["+" @ $count++]

  ? $count > 10
    p.warning["Getting high!"]
